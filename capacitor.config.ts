import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'React Swipe Back',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
